# Contributing to MA-CLI
- Python 3.11+.
- Run pytest, ruff, and mypy before submitting changes.
- Never commit credentials, generated distributions, or local workspaces.
- Security-sensitive changes require regression tests.
